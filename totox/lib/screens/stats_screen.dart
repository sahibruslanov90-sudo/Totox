import 'package:flutter/material.dart';
import '../services/match_service.dart';
class StatsScreen extends StatelessWidget { const StatsScreen({super.key}); @override Widget build(BuildContext context)=>SafeArea(child:ListView(padding:const EdgeInsets.all(20),children:[const Text('İstatistik',style:TextStyle(fontSize:26,fontWeight:FontWeight.w900)),const SizedBox(height:6),const Text('Haftanın maçları için özet dağılım',style:TextStyle(color:Colors.white54)),const SizedBox(height:18),...MatchService.matches.map((m)=>Card(margin:const EdgeInsets.only(bottom:8),child:Padding(padding:const EdgeInsets.all(14),child:Row(children:[SizedBox(width:34,child:Text('${m.number}',style:const TextStyle(color:Colors.greenAccent,fontWeight:FontWeight.bold))),Expanded(child:Text('${m.home} - ${m.away}',style:const TextStyle(fontWeight:FontWeight.w600))),Text('1 %${m.homeForm}',style:const TextStyle(fontSize:11)),const SizedBox(width:8),Text('X %${m.drawForm}',style:const TextStyle(fontSize:11)),const SizedBox(width:8),Text('2 %${m.awayForm}',style:const TextStyle(fontSize:11))])))])); }
