import 'package:flutter/material.dart';
import '../models/match.dart';
import '../theme/app_theme.dart';
import '../widgets/logo.dart';
class MatchDetailScreen extends StatelessWidget { final FootballMatch match; const MatchDetailScreen({super.key,required this.match}); @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Maç Analizi')),body:ListView(padding:const EdgeInsets.all(20),children:[Row(children:[Expanded(child:Column(children:[TeamLogo(name:match.home),const SizedBox(height:8),Text(match.home,textAlign:TextAlign.center,style:const TextStyle(fontWeight:FontWeight.bold))])),const Column(children:[Text('VS',style:TextStyle(color:Colors.white38)),SizedBox(height:5),Text('20:00',style:TextStyle(fontWeight:FontWeight.bold))]),Expanded(child:Column(children:[TeamLogo(name:match.away),const SizedBox(height:8),Text(match.away,textAlign:TextAlign.center,style:const TextStyle(fontWeight:FontWeight.bold))]))]),const SizedBox(height:24),Card(child:Padding(padding:const EdgeInsets.all(18),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('İstatistiksel dağılım',style:TextStyle(fontSize:18,fontWeight:FontWeight.w900)),const SizedBox(height:16),_bar('1',match.homeForm),_bar('X',match.drawForm),_bar('2',match.awayForm)]))),const SizedBox(height:12),Card(child:Padding(padding:const EdgeInsets.all(18),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('Form özeti',style:TextStyle(fontSize:18,fontWeight:FontWeight.w900)),const SizedBox(height:10),Text('${match.home}: son 5 maç verisi API bağlandığında gösterilecek.'),Text('${match.away}: son 5 maç verisi API bağlandığında gösterilecek.',style:const TextStyle(color:Colors.white60))]))),const SizedBox(height:12),const Card(child:Padding(padding:EdgeInsets.all(18),child:Text('Bu ekran MVP için örnek istatistik dağılımı kullanır. Gerçek veriler API entegrasyonundan sonra otomatik güncellenecektir.',style:TextStyle(color:Colors.white60))))])); }
Widget _bar(String label,int value)=>Padding(padding:const EdgeInsets.only(bottom:12),child:Row(children:[SizedBox(width:25,child:Text(label,style:const TextStyle(fontWeight:FontWeight.bold))),Expanded(child:ClipRRect(borderRadius:BorderRadius.circular(8),child:LinearProgressIndicator(value:value/100,minHeight:10,color:AppTheme.green,backgroundColor:Colors.white10))),const SizedBox(width:10),Text('%$value')])));
}
